1. 환경 설정 (Requirements)
pip install pdf2image pillow google-generativeai

Windows: poppler-for-windows에서 다운로드 후 환경변수 PATH에 추가
https://github.com/oschwartz10612/poppler-windows/releases

pip install pymupdf

pip install django

2단계: 장고 프로젝트 생성 및 설계
# 1. 'my_quiz_site'라는 이름의 프로젝트 생성
django-admin startproject my_quiz_site

# 2. 생성된 폴더 안으로 들어가기
cd my_quiz_site

# 3. 'quiz'라는 실제 기능을 담당할 앱 생성
python manage.py startapp quiz


3. 설계도를 실제 DB에 반영하기 (Migration)

# 1. 설계도(Migration) 만들기
python manage.py makemigrations quiz

# 2. 실제 DB에 적용하기
python manage.py migrate


네, 맞습니다! 아까 pip install django를 입력하셨다면 컴퓨터에 장고(Django)라는 도구 상자는 이미 들어와 있는 상태입니다.

하지만 **'도구(Django)'**를 설치한 것과 그 도구로 **'내 프로젝트(웹 서버)'**를 만드는 것은 별개입니다. 비유하자면, 아까는 망치와 톱(Django)을 사온 것이고, 이제 그 연장을 꺼내서 **진짜 집(문제은행 서버)**을 지어야 합니다.

2단계: 장고 프로젝트 생성 및 설계
이제 내 컴퓨터를 '문제은행 서버'로 만들기 위한 구체적인 명령어를 실행해 보겠습니다.

1. 프로젝트 집 짓기 (폴더 생성)
CMD(명령 프롬프트) 창을 열고, 아까 작업하던 폴더로 이동한 뒤 아래 명령어를 순서대로 입력하세요.

Bash
# 1. 'my_quiz_site'라는 이름의 프로젝트 생성
django-admin startproject my_quiz_site

# 2. 생성된 폴더 안으로 들어가기
cd my_quiz_site

# 3. 'quiz'라는 실제 기능을 담당할 앱 생성
python manage.py startapp quiz

4. 관리자 계정 만들기 (중요!)
python manage.py createsuperuser
Username: (영문 이름 입력)

Email: (그냥 엔터 쳐도 됨)

Password: (비밀번호 입력 - 화면에 글자가 안 보이지만 입력되고 있는 것이니 끝까지 치고 엔터 누르세요!)

5. 서버 실행해서 확인하기
python manage.py runserver

브라우저 주소창에 http://127.0.0.1:8000/admin을 입력하고 아까 만든 계정으로 로그인해 보세요.



💡 현재까지 완료된 상태 체크
my_quiz_site 프로젝트 생성 완료

quiz 앱 생성 및 models.py 작성 완료

python manage.py migrate로 db.sqlite3 파일 생성 완료

createsuperuser로 관리자 계정 생성 완료


1. 업로드 스크립트 작성 (db_uploader.py)
manage.py 파일이 있는 가장 바깥 폴더에 아래 파일을 새로 만드세요. (파일명: db_uploader.py)

Python
import os
import json
import django

# 1. Django 환경 설정 (내 프로젝트 이름을 가져옵니다)
os.environ.setdefault("DJANGO_SETTINGS_MODULE", "my_quiz_site.settings")
django.setup()

from quiz.models import Question, Choice

def insert_quiz_data(json_file_path):
    # 2. JSON 파일 읽기
    with open(json_file_path, 'r', encoding='utf-8') as f:
        data = json.load(f)

    print(f"{len(data)}개의 데이터를 DB에 넣기 시작합니다...")

    for item in data:
        # 3. Question(문제) 저장
        q = Question.objects.create(
            number=int(item['no']),
            content=item['content']
        )

        # 4. Choice(보기) 저장
        # 객관식 보기가 있는 경우에만 실행합니다.
        for opt in item.get('options', []):
            Choice.objects.create(
                question=q,
                choice_text=opt
            )
        
    print("모든 데이터가 성공적으로 저장되었습니다!")

if __name__ == "__main__":
    # 아까 생성된 JSON 파일 이름을 여기에 적으세요!
    target_json = "result_파일명.json" 
    insert_quiz_data(target_json)
2. 실행하기 (CMD 창)
Bash
python db_uploader.py
🖥️ 결과 확인 (웹 브라우저)
데이터를 다 넣으셨다면, 다시 서버를 켜고 관리자 페이지에서 확인해 보세요.

python manage.py runserver 실행

브라우저에서 http://127.0.0.1:8000/admin 접속





